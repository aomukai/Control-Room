# Gradle
.gradle/
**/.gradle/

# Build output
build/
**/build/

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# Claude Code
.claude/

nul

# Design Documents
# Everything in docs/ is private and must not be uploaded to GitHub. It's critical infrastructure; keep local.
docs/

# Local agent registry data
workspace/**/.control-room/agents/*.json

# Local runtime data
data/*.json
data/*.wav

# Piper TTS voice models (downloaded via setup-piper.ps1)
data/voices/

# Local workspace UI state
workspace/**/.controlroom/state/*.json

# Workspace
workspace/
# === PRIVATE DEV PHASE (INTENTIONAL) ===
# docs/ is *not* documentation right now. It contains private, operational project infrastructure.
# Nothing in docs/ may be pushed to GitHub.
